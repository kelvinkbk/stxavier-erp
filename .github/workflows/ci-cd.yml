name: 🚀 St. Xavier ERP - CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '18'

jobs:
  # Code Quality & Testing
  quality-check:
    name: 🔍 Code Quality & Security
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🛡️ Security audit
        run: npm audit --audit-level moderate
        continue-on-error: true

      - name: 📝 TypeScript check
        run: npx tsc --noEmit

      - name: 🧹 Lint check
        run: npm run lint
        continue-on-error: true

      - name: 🧪 Run tests
        run: npm test
        continue-on-error: true

  # Web Build & Deploy
  web-deploy:
    name: 🌐 Web Build & Deploy
    runs-on: ubuntu-latest
    needs: quality-check
    if: github.ref == 'refs/heads/main'

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🏗️ Build web version (Demo Mode)
        if: ${{ secrets.FIREBASE_API_KEY == '' }}
        run: |
          echo "🔧 Building in demo mode (Firebase secrets not configured)"
          npm run build:web
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: demo-api-key
          EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN: demo-project.firebaseapp.com
          EXPO_PUBLIC_FIREBASE_PROJECT_ID: demo-project
          EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET: demo-project.appspot.com
          EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: 123456789
          EXPO_PUBLIC_FIREBASE_APP_ID: demo-app-id
          EXPO_PUBLIC_ENVIRONMENT: demo

      - name: 🏗️ Build web version (Production)
        if: ${{ secrets.FIREBASE_API_KEY != '' }}
        run: npm run build:web
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          EXPO_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          EXPO_PUBLIC_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          EXPO_PUBLIC_ENVIRONMENT: production

      - name: 🚀 Deploy to Vercel
        if: ${{ secrets.VERCEL_TOKEN != '' }}
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
          working-directory: ./

      - name: ⚠️ Deployment skipped
        if: ${{ secrets.VERCEL_TOKEN == '' }}
        run: |
          echo "🔧 Deployment skipped: Vercel secrets not configured"
          echo "📝 To enable deployment, add these secrets to your repository:"
          echo "   - VERCEL_TOKEN"
          echo "   - ORG_ID"
          echo "   - PROJECT_ID"
          echo "   - FIREBASE_API_KEY"
          echo "   - FIREBASE_AUTH_DOMAIN"
          echo "   - FIREBASE_PROJECT_ID"
          echo "   - FIREBASE_STORAGE_BUCKET"
          echo "   - FIREBASE_MESSAGING_SENDER_ID"
          echo "   - FIREBASE_APP_ID"

      - name: 📦 Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: web-build
          path: dist/
          retention-days: 7

  # Mobile Build Check
  mobile-check:
    name: 📱 Mobile Build Check
    runs-on: ubuntu-latest
    needs: quality-check

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 📱 Expo Doctor
        run: npx expo doctor
        continue-on-error: true

      - name: 🏗️ Check Expo build (Demo Mode)
        if: ${{ secrets.FIREBASE_API_KEY == '' }}
        run: npx expo export --platform all --output-dir build-check
        continue-on-error: true
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: demo-api-key
          EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN: demo-project.firebaseapp.com
          EXPO_PUBLIC_FIREBASE_PROJECT_ID: demo-project
          EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET: demo-project.appspot.com
          EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: 123456789
          EXPO_PUBLIC_FIREBASE_APP_ID: demo-app-id
          EXPO_PUBLIC_ENVIRONMENT: demo

      - name: 🏗️ Check Expo build (Production)
        if: ${{ secrets.FIREBASE_API_KEY != '' }}
        run: npx expo export --platform all --output-dir build-check
        continue-on-error: true
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          EXPO_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          EXPO_PUBLIC_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          EXPO_PUBLIC_ENVIRONMENT: production

  # Performance Monitoring
  performance-check:
    name: ⚡ Performance Analysis
    runs-on: ubuntu-latest
    needs: web-deploy

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 📦 Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: web-build
          path: dist/

      - name: 📊 Bundle size analysis
        run: |
          echo "📦 Analyzing bundle size..."
          du -sh dist/
          find dist/ -name "*.js" -exec wc -c {} + | sort -n | tail -10
          echo "📊 Top 10 largest JavaScript files:"
          find dist/ -name "*.js" -exec ls -lah {} + | sort -k5 -hr | head -10
        continue-on-error: true

      - name: ⚡ Performance test
        run: npm run performance:test
        continue-on-error: true

  # Security Scan
  security-scan:
    name: 🛡️ Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
        continue-on-error: true

      - name: 📤 Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v2
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
        continue-on-error: true

  # Notification
  notify:
    name: 📢 Deployment Notification
    runs-on: ubuntu-latest
    needs: [web-deploy, mobile-check, performance-check, security-scan]
    if: always() && github.ref == 'refs/heads/main'

    steps:
      - name: 📧 Notify deployment status
        run: |
          echo "🎉 St. Xavier ERP deployment pipeline completed!"
          echo "✅ Web: ${{ needs.web-deploy.result }}"
          echo "📱 Mobile: ${{ needs.mobile-check.result }}"
          echo "⚡ Performance: ${{ needs.performance-check.result }}"
          echo "🛡️ Security: ${{ needs.security-scan.result }}"
          echo ""
          if [[ "${{ secrets.FIREBASE_API_KEY }}" != "" ]]; then
            echo "🔥 Firebase: Production configuration detected"
          else
            echo "🔧 Firebase: Demo mode (add secrets for production)"
          fi
          if [[ "${{ secrets.VERCEL_TOKEN }}" != "" ]]; then
            echo "🚀 Vercel: Deployment enabled"
          else
            echo "📝 Vercel: Add VERCEL_TOKEN secret to enable deployment"
          fi
